import 'package:flutter/material.dart';

class NewScreen extends StatelessWidget {
  const NewScreen({super.key});

  static const String route = '/newScreen';

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
