import 'package:flutter/material.dart';
import 'package:pey/src/features/users/data/models/user.dart';
import '../storage/dispatch.dart';

Future<void> profileNavigator(
  BuildContext context,
  User user,
) async {
  if (user != getUser) {
  } else {}
}
